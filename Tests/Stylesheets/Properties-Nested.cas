//some properties have child properties. this is only used for text attributes at moment, but could be utilised for other things in the future.
//to differentiate these from selectors you need to mark them with an @ symbol, `{` and `}` are optional

UIButton
  background-color:#fff;
  border-width 1
  > UINavigationBar
    amazing 23
  embed @
    font arial 15
    paragraphStyle @{
      maximumLineHeight 2
      minimumLineHeight 4
    }
    kern 5
    stuff: "1 more thing"
  UISlider
    another 34